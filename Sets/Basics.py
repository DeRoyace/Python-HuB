# Sets are collection of unique elements (i.e non-repetitive)
# It is unordered
# It cannot be access by index
# We cannot edit set items; but we can add & remove items
# We cannot have duplicate items

setVal = {786, 98.76, "Rails", "Rust", True}
print(setVal)
print("Type of variable setVal:", type(setVal))
print("Length of the set:", len(setVal))